#include "main.h"

/**
 * _isupper - print 1 if its uppercase 0 otherwise
 * @c: reacd character
 * Return: 1 or 0
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
